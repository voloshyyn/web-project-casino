function createJob(req, res) {
  res.status(501).json({ message: 'Not implemented yet' });
}

function listJobs(req, res) {
  res.status(501).json({ message: 'Not implemented yet' });
}

module.exports = {
  createJob,
  listJobs
};
